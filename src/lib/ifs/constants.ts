/** Constantes seguras para Edge (middleware) y Node. */
export const SESSION_COOKIE = "hmv_ifs_session";

/** Impersonación UAT (?u=): cookie httpOnly firmada. No es autenticación. */
export const IMPERSONATE_COOKIE = "hmv_impersonate";
