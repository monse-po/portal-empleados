# Doble clic / terminal → OCI PROD (Bastion por API).

El `.env` IFS de la VM PROD no se copia de DEV. IAM de producción registra su propio `redirect_uri`.

Ver [`../README.md`](../README.md).

```bash
./deploy-via-bastion.sh deploy
```
